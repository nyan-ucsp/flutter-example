import 'package:flutter_example/database/database.dart';

class CategoryWithProductCount {
  final Category category;
  final int productCount;

  CategoryWithProductCount(this.category, this.productCount);
}